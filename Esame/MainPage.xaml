<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Esame.MainPage">

    <ScrollView>
        <VerticalStackLayout>
            <Button Text="Visualizza Carrello" 
            Clicked="OnViewCartClicked" 
            HorizontalOptions="Center" 
            BackgroundColor="Blue" 
            TextColor="White" />
            <CollectionView x:Name="MyCollectionView" SelectionMode="Single"  SelectionChanged="OnSelectionChanged" ItemsSource="{Binding Items}" Margin="50">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout Margin="50">
                            <Image Source="{Binding Image}" Margin="50" 
                                       Aspect="AspectFit" 
                                       HeightRequest="200" 
                                       WidthRequest="200" 
                                       HorizontalOptions="Center" />

                            <!-- Titolo del prodotto -->
                            <Label Text="{Binding Title}" 
                                       FontSize="18" 
                                       FontAttributes="Bold" 
                                       TextColor="White" 
                                       HorizontalOptions="Center" />

                            <!-- Prezzo del prodotto -->
                            <Label Text="{Binding Price, StringFormat='${0:F2}'}" 
                                       FontSize="16" 
                                       FontAttributes="Italic" 
                                       TextColor="Green" 
                                       HorizontalOptions="Center" />

                            <!-- Categoria del prodotto -->
                            <Label Text="{Binding Category}" 
                                       FontSize="14" 
                                       TextColor="Gray" 
                                       HorizontalOptions="Center" />
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
